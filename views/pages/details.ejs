<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("../partials/header") %>
  </head>
  <body class="bg-gray-900 flex items-center justify-center min-h-screen">
    <div class="bg-red-700 shadow-2xl border-8 border-red-900 rounded-2xl p-6 w-full max-w-md text-white relative">
   
      <div class="absolute top-3 left-3 flex space-x-2">
        <div class="w-5 h-5 bg-green-400 rounded-full shadow-lg animate-pulse"></div>
        <div class="w-4 h-4 bg-yellow-400 rounded-full shadow-md"></div>
        <div class="w-3 h-3 bg-red-500 rounded-full shadow-sm"></div>
      </div>

    
      <h1 class="text-3xl font-bold text-center mb-5 capitalize border-b border-gray-300 pb-3">
        <%= pokemon.name %>
      </h1>

      
      <div class="bg-green-300 rounded-lg p-4 shadow-inner flex flex-col items-center">
        <img
          src="<%= pokemon.sprites.front_default %>"
          alt="<%= pokemon.name %>"
          class="w-40 h-40"
        />
      </div>

      
      <div class="mt-4 space-y-2 text-black bg-gray-100 p-4 rounded-lg shadow-inner">
        <p class="text-lg font-semibold flex items-center">
          <svg class="w-5 h-5 inline-block mr-2 text-gray-700" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 9l4-4 4 4m0 6l-4 4-4-4" />
          </svg>
          Height: <%= pokemon.height %>
        </p>
        <p class="text-lg font-semibold flex items-center">
          <svg class="w-5 h-5 inline-block mr-2 text-gray-700" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 4H5m14-8H5" />
          </svg>
          Weight: <%= pokemon.weight %>
        </p>
        <p class="text-lg font-semibold flex items-center">
          <svg class="w-5 h-5 inline-block mr-2 text-gray-700" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5z" />
          </svg>
          Type: <%= pokemon.types.map(t => t.type.name).join(', ') %>
        </p>
      </div>

      
      <div class="text-center mt-5">
        <a href="/" class="px-6 py-3 bg-gray-800 text-white rounded-full shadow-md hover:bg-gray-900 transition">
          Back
        </a>
      </div>
    </div>

    <%- include("../partials/footer") %>
  </body>
</html>
